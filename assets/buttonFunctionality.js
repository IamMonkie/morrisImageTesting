/* ----------------------------- runs at startup ---------------------------- */
document.addEventListener("DOMContentLoaded", (e) => {
  console.log("Document is Ready");

  /* -------------------------------- Variables ------------------------------- */
  let i = 0;
  let numOfImageSlots = 1;

  //   let sort = document.getElementById("buttonContainer", "iconContainer");
  /* --------------------------------- Scripts -------------------------------- */

  /* -------------------------------- Functions ------------------------------- */

  // imageSlots
  function addImageSlots() {
    for (; i < length; i++) {}
  }

  //Sort
});
